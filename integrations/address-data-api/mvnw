#!/bin/sh
# Maven wrapper script for Unix-like systems

MAVEN_VERSION=3.8.6
MAVEN_HOME=./.mvn/wrapper/maven-wrapper.jar
MAVEN_OPTS="-Xmx1024m"

if [ -n "$JAVA_HOME" ]; then
  JAVA_CMD="$JAVA_HOME/bin/java"
else
  JAVA_CMD=java
fi

exec "$JAVA_CMD" $MAVEN_OPTS -jar "$MAVEN_HOME" "$@"